<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen p-4">
    <div class="max-w-2xl mx-auto bg-white p-8 rounded-lg shadow-xl">
      <h1 class="text-3xl font-bold text-blue-800 mb-6 border-b-2 border-blue-500 pb-3">
        <%= title %>
      </h1>

      <% if (course && course.id) { %>
        <!-- UPDATE FORM -->
        <form action="/courses/<%= course.id %>/update" method="POST" class="space-y-6">
          <div>
            <label for="disciplineId" class="block text-sm font-medium text-gray-700 mb-2">
              Discipline
            </label>
            <select
              id="disciplineId"
              name="disciplineId"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            >
              <% if (disciplines && disciplines.length > 0) { %>
                <% disciplines.forEach(d => { %>
                  <option value="<%= d.id %>" <%= d.id == course.discipline_id ? 'selected' : '' %>>
                    <%= d.name %>
                  </option>
                <% }) %>
              <% } %>
            </select>
          </div>

          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
              Course Name
            </label>
            <input
              type="text"
              id="name"
              name="name"
              value="<%= course.name %>"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            />
          </div>

          <div>
            <label for="description" class="block text-sm font-medium text-gray-700 mb-2">
              Description
            </label>
            <textarea
              id="description"
              name="description"
              rows="3"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            ><%= course.description %></textarea>
          </div>

          <div>
            <label for="mainContent" class="block text-sm font-medium text-gray-700 mb-2">
              Main Content / Topics
            </label>
            <textarea
              id="mainContent"
              name="mainContent"
              rows="4"
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            ><%= course.main_content || '' %></textarea>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div>
              <label for="difficulty" class="block text-sm font-medium text-gray-700 mb-2">
                Difficulty (1-10)
              </label>
              <input
                type="number"
                id="difficulty"
                name="difficulty"
                min="1"
                max="10"
                value="<%= course.difficulty %>"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
              />
            </div>
            <div>
              <label for="rewards" class="block text-sm font-medium text-gray-700 mb-2">
                Rewards (1-10)
              </label>
              <input
                type="number"
                id="rewards"
                name="rewards"
                min="1"
                max="10"
                value="<%= course.rewards %>"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
              />
            </div>
          </div>

          <div>
            <label for="quantityAvailable" class="block text-sm font-medium text-gray-700 mb-2">
              Quantity Available
            </label>
            <input
              type="number"
              id="quantityAvailable"
              name="quantityAvailable"
              min="0"
              value="<%= course.quantity_available || 0 %>"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500"
            />
          </div>

          <div class="flex gap-4 pt-4">
            <button
              type="submit"
              class="px-6 py-2 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition duration-200"
            >
              Update Course
            </button>
            <a
              href="/courses/<%= course.id %>"
              class="px-6 py-2 bg-gray-400 text-white font-semibold rounded-lg hover:bg-gray-500 transition duration-200"
            >
              Cancel
            </a>
          </div>
        </form>
      <% } else { %>
        <!-- CREATE FORM -->
        <form action="/disciplines/<%= discipline.id %>/courses/create" method="POST" class="space-y-6">
          <div class="bg-blue-50 p-4 rounded-lg border border-blue-200">
            <p class="text-sm text-gray-700">
              <strong>Creating course for:</strong> <span class="text-blue-700 font-bold"><%= discipline.name %></span>
            </p>
          </div>

          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
              Course Name *
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              placeholder="e.g., Power Systems Fundamentals"
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
            />
          </div>

          <div>
            <label for="description" class="block text-sm font-medium text-gray-700 mb-2">
              Description *
            </label>
            <textarea
              id="description"
              name="description"
              rows="3"
              required
              placeholder="Brief description of the course..."
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
            ></textarea>
          </div>

          <div>
            <label for="mainContent" class="block text-sm font-medium text-gray-700 mb-2">
              Main Content / Topics
            </label>
            <textarea
              id="mainContent"
              name="mainContent"
              rows="4"
              placeholder="Key topics covered in this course, comma-separated..."
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
            ></textarea>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div>
              <label for="difficulty" class="block text-sm font-medium text-gray-700 mb-2">
                Difficulty (1-10) *
              </label>
              <input
                type="number"
                id="difficulty"
                name="difficulty"
                min="1"
                max="10"
                value="5"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
              />
            </div>
            <div>
              <label for="rewards" class="block text-sm font-medium text-gray-700 mb-2">
                Rewards (1-10) *
              </label>
              <input
                type="number"
                id="rewards"
                name="rewards"
                min="1"
                max="10"
                value="5"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
              />
            </div>
          </div>

          <div>
            <label for="quantityAvailable" class="block text-sm font-medium text-gray-700 mb-2">
              Initial Quantity Available
            </label>
            <input
              type="number"
              id="quantityAvailable"
              name="quantityAvailable"
              min="0"
              value="0"
              class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500"
            />
          </div>

          <div class="flex gap-4 pt-4">
            <button
              type="submit"
              class="px-6 py-2 bg-green-600 text-white font-semibold rounded-lg hover:bg-green-700 transition duration-200"
            >
              Create Course
            </button>
            <a
              href="/disciplines/<%= discipline.id %>"
              class="px-6 py-2 bg-gray-400 text-white font-semibold rounded-lg hover:bg-gray-500 transition duration-200"
            >
              Cancel
            </a>
          </div>
        </form>
      <% } %>
    </div>
  </body>
</html>