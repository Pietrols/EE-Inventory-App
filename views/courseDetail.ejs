<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen p-4 md:p-8">
    <div class="max-w-4xl mx-auto bg-white p-6 md:p-10 rounded-xl shadow-2xl">
      <div class="mb-8 border-b-4 border-blue-500 pb-6">
        <div class="flex justify-between items-start">
          <div class="flex-1">
            <h1 class="text-4xl font-extrabold text-blue-800 mb-3">
              <%= course.name %>
            </h1>
            <p class="text-lg text-gray-600">
              <span class="font-semibold">Discipline:</span>
              <a
                href="/disciplines/<%= discipline.id %>"
                class="text-blue-600 hover:text-blue-800 underline"
              >
                <%= discipline.name %>
              </a>
            </p>
          </div>
          <div class="flex flex-col gap-2 ml-4">
            <a
              href="/courses/<%= course.id %>/update"
              class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition whitespace-nowrap text-center font-semibold"
            >
              Edit Course
            </a>
            <form
              action="/courses/<%= course.id %>/delete"
              method="POST"
              style="display: inline"
            >
              <button
                type="submit"
                onclick="return confirm('Delete this course? This action cannot be undone.')"
                class="w-full px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition font-semibold"
              >
                Delete Course
              </button>
            </form>
          </div>
        </div>
      </div>

      <div class="grid md:grid-cols-3 gap-8 mb-8">
        <div class="md:col-span-2">
          <h2 class="text-2xl font-bold text-gray-800 mb-4">Description</h2>
          <p class="text-gray-700 leading-relaxed text-lg mb-6">
            <%= course.description %>
          </p>

          <h2 class="text-2xl font-bold text-gray-800 mb-4">Course Content</h2>
          <div class="bg-gray-50 p-6 rounded-lg border border-gray-300">
            <p class="text-gray-700 whitespace-pre-wrap">
              <%= course.main_content || 'No content specified yet.' %>
            </p>
          </div>
        </div>

        <div>
          <div
            class="bg-gradient-to-br from-blue-50 to-blue-100 p-6 rounded-lg border-2 border-blue-200"
          >
            <h3 class="text-xl font-bold text-gray-800 mb-5">Course Metrics</h3>

            <div class="space-y-4">
              <div>
                <p class="text-sm font-semibold text-gray-600 mb-1">
                  Difficulty Level
                </p>
                <div class="flex items-center gap-2">
                  <div class="flex-1 bg-gray-300 rounded-full h-2">
                    <div
                      class="bg-orange-500 h-2 rounded-full"
                      style="width: <%= (course.difficulty / 10) * 100 %>%"
                    ></div>
                  </div>
                  <span class="font-bold text-orange-600 text-lg"
                    ><%= course.difficulty %>/10</span
                  >
                </div>
              </div>

              <div>
                <p class="text-sm font-semibold text-gray-600 mb-1">Rewards</p>
                <div class="flex items-center gap-2">
                  <div class="flex-1 bg-gray-300 rounded-full h-2">
                    <div
                      class="bg-green-500 h-2 rounded-full"
                      style="width: <%= (course.rewards / 10) * 100 %>%"
                    ></div>
                  </div>
                  <span class="font-bold text-green-600 text-lg"
                    ><%= course.rewards %>/10</span
                  >
                </div>
              </div>

              <div class="border-t border-blue-200 pt-4">
                <p class="text-sm font-semibold text-gray-600 mb-1">
                  Quantity Available
                </p>
                <p class="text-3xl font-bold text-blue-600">
                  <%= course.quantity_available %>
                </p>
              </div>

              <% if (course.rating) { %>
              <div class="border-t border-blue-200 pt-4">
                <p class="text-sm font-semibold text-gray-600 mb-1">
                  Course Rating
                </p>
                <p class="text-2xl font-bold text-yellow-500">
                  <%= course.rating %>/5 ⭐
                </p>
              </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <div class="flex flex-wrap gap-4 pt-6 border-t">
        <a
          href="/disciplines/<%= discipline.id %>"
          class="px-6 py-2 bg-gray-500 text-white rounded-lg hover:bg-gray-600 transition font-semibold"
        >
          ← Back to <%= discipline.name %>
        </a>
        <a
          href="/disciplines"
          class="px-6 py-2 bg-gray-400 text-white rounded-lg hover:bg-gray-500 transition font-semibold"
        >
          ← All Disciplines
        </a>
      </div>
    </div>
  </body>
</html>
