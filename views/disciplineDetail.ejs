<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 min-h-screen p-4 md:p-8">
    <div class="max-w-6xl mx-auto bg-white p-6 md:p-10 rounded-xl shadow-2xl">
      <div
        class="flex justify-between items-start mb-6 border-b-4 border-blue-500 pb-4"
      >
        <div class="flex-1">
          <h1 class="text-4xl font-extrabold text-blue-800">
            <%= discipline.name %>
          </h1>
          <p class="text-gray-600 mt-3 text-lg">
            <%= discipline.description %>
          </p>
        </div>
        <div class="flex flex-col gap-2 ml-4">
          <a
            href="/disciplines/<%= discipline.id %>/update"
            class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition whitespace-nowrap text-center"
          >
            Edit Discipline
          </a>
          <form
            action="/disciplines/<%= discipline.id %>/delete"
            method="POST"
            style="display: inline"
          >
            <button
              type="submit"
              onclick="return confirm('Delete this discipline and all its courses? This cannot be undone.')"
              class="w-full px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition"
            >
              Delete Discipline
            </button>
          </form>
        </div>
      </div>

      <div class="mb-8">
        <div class="flex justify-between items-center mb-6">
          <h2 class="text-2xl font-bold text-gray-800">
            Courses in this Discipline
          </h2>
          <a
            href="/disciplines/<%= discipline.id %>/courses/create"
            class="px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 transition font-semibold"
          >
            + Add New Course
          </a>
        </div>

        <% if (courses && courses.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% courses.forEach(course => { %>
          <div
            class="bg-white border-2 border-gray-300 rounded-lg hover:shadow-xl transition duration-300 overflow-hidden"
          >
            <div class="p-5">
              <a
                href="/courses/<%= course.id %>"
                class="text-xl font-bold text-blue-700 hover:text-blue-900 block mb-2"
              >
                <%= course.name %>
              </a>
              <p class="text-sm text-gray-600 mb-4">
                <%= course.description.substring(0, 100) %><% if
                (course.description.length > 100) { %>...<% } %>
              </p>

              <div class="space-y-2 mb-4">
                <div class="flex justify-between text-sm">
                  <span class="font-semibold text-gray-700">Difficulty:</span>
                  <span class="text-orange-600 font-bold"
                    ><%= course.difficulty %>/10</span
                  >
                </div>
                <div class="flex justify-between text-sm">
                  <span class="font-semibold text-gray-700">Rewards:</span>
                  <span class="text-green-600 font-bold"
                    ><%= course.rewards %>/10</span
                  >
                </div>
                <div class="flex justify-between text-sm">
                  <span class="font-semibold text-gray-700">Quantity:</span>
                  <span class="text-blue-600 font-bold"
                    ><%= course.quantity_available %></span
                  >
                </div>
              </div>

              <div class="flex gap-2 mt-4">
                <a
                  href="/courses/<%= course.id %>"
                  class="flex-1 px-3 py-2 bg-blue-500 text-white text-sm rounded hover:bg-blue-600 transition text-center font-semibold"
                >
                  View
                </a>
                <a
                  href="/courses/<%= course.id %>/update"
                  class="flex-1 px-3 py-2 bg-yellow-500 text-white text-sm rounded hover:bg-yellow-600 transition text-center font-semibold"
                >
                  Edit
                </a>
                <form
                  action="/courses/<%= course.id %>/delete"
                  method="POST"
                  style="display: inline; flex: 1"
                >
                  <button
                    type="submit"
                    onclick="return confirm('Delete this course?')"
                    class="w-full px-3 py-2 bg-red-500 text-white text-sm rounded hover:bg-red-600 transition font-semibold"
                  >
                    Delete
                  </button>
                </form>
              </div>
            </div>
          </div>
          <% }) %>
        </div>
        <% } else { %>
        <div
          class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-6 rounded-md"
        >
          <p class="font-bold text-lg mb-2">No Courses Yet</p>
          <p>
            This discipline doesn't have any courses. Click "Add New Course" to
            create one.
          </p>
        </div>
        <% } %>
      </div>

      <div class="mt-8 flex gap-4">
        <a
          href="/disciplines"
          class="px-6 py-2 bg-gray-500 text-white rounded-lg hover:bg-gray-600 transition font-semibold"
        >
          ← Back to All Disciplines
        </a>
      </div>
    </div>
  </body>
</html>
